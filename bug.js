```javascript
// Example usage in a component
<div className="bg-gradient-to-r from-blue-500 to-purple-500 p-4 rounded-lg shadow-lg">
  <p className="text-white text-xl font-bold">This is a gradient background</p>
</div>
```

This code uses Tailwind's gradient feature, but the gradients might not render correctly if there is a conflict with other CSS or if the Tailwind configuration is not set up properly. 